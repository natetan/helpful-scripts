/*
 * Definitions takes a line-by-line text file of terms in the format of 
 * term: definition
 */

const fs = require('fs'); // Read and write files
const readline = require('readline') // Reads files line by like

